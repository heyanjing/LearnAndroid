<?xml version="1.0" encoding="utf-8"?>
<!--
http://blog.csdn.net/iispring/article/details/50437731
-->
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <!--半径
    radius指定四个角的半径
    topRightRadius,
    topLeftRadius,
    bottomRightRadius,
    bottomLeftRadius指定具体某个角的半径
    -->
    <corners android:radius="40dp"/>
    <!--内部单色填充
    <solid android:color="#ff0000"></solid>
    -->
    <!--尺寸-->
    <size
        android:width="40dp"
        android:height="40dp"/>
    <!-- 内边距-->
    <padding
        android:bottom="2dp"
        android:left="2dp"
        android:right="2dp"
        android:top="2dp"/>
    <!--描边
    width
    dashWidth  线的长度
    dashGap  线与线之间的间距
    -->
    <stroke
        android:width="1dp"
        android:color="#00ff00"
        android:dashGap="3dp"
        android:dashWidth="20dp"/>
    <!--
    type 值有  linear（线性渐变） radial(.圆形渐变，起始颜色从cenralX,centralY点开始) sweep（锥形）
    startColor
    centerColor
    endColor

    angle 代表渐变颜色的角度， 0 is left to right, 90 is bottom to top. 必须是45的整数倍. 默认是 0.该属性只有在type=linear情况下起作用，默认的type为linear

   centerX  指定起始点，这两个属性只有在type不为linear情况下起作用
   centerY  指定起始点，这两个属性只有在type不为linear情况下起作用
   gradientRadius 渐变颜色的半径  如果android:type="radial"，没有设置android:gradientRadius，将会报错
    -->
    <gradient
        android:centerColor="#00ff00"
        android:endColor="#0000ff"
        android:startColor="#ff0000"
        android:gradientRadius="30dp"
        android:centerX="0.5"
        android:centerY="0.5"
        android:type="sweep"/>

</shape>